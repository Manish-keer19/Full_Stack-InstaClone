{
  "compilerOptions": {
    "outDir": "./dist",  // Make sure output goes to 'dist' folder
    "target": "ES6",
    "module": "CommonJS",
    "esModuleInterop": true
  },
  "include": [
    "src/**/*.ts"  // Ensure all TypeScript files are included
  ]
}
